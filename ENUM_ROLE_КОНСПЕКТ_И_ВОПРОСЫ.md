## 🧨 ENUM `Role` — конспект, разбор и твои ответы

Этот файл — чтобы ты мог потом открыть, пробежать глазами и вспомнить, **нахрена мы вообще городили этот `Role` и enum в целом**.

---

## 1. Зачем вообще нужен `Role`

В проекте есть три типа пользователей:

- **COURIER** — курьер, который бегает и доставляет заказы.
- **SHOP** — магазин, который создаёт заказы и следит за доставкой.
- **ADMIN** — админ, который рулит всей движухой (активация, блокировки, штрафы, комиссии).

В БД мы заранее ограничили колонку `role`:

```sql
role VARCHAR(20) CHECK (role IN ('COURIER', 'SHOP', 'ADMIN'))
```

В Java мы хотим **то же самое ограничение**, но на уровне кода — чтобы нельзя было писать рандомные строки.

**Enum `Role` = тип, который говорит:**
> «Роль может быть только одной из: COURIER, SHOP, ADMIN. Никаких `BOSS` и `PIMP` без моего ведома».

---

## 2. Что такое `enum` по-человечески

**Enum (перечисление)** — это тип с **фиксированным набором значений**.

Примеры из жизни:

- Дни недели: `MONDAY, TUESDAY, ...`
- Статус заказа: `NEW, IN_PROGRESS, DELIVERED`
- Роль пользователя: `COURIER, SHOP, ADMIN`

Без enum ты бы писал так:

```java
String role = "COURIER";
```

И в этом аду возможны:

- `"COURIR"` → опечатка, компилятор молчит.
- `"ADMIN "` (с пробелом) → сравнение не сработает, а ты будешь смотреть в монитор как брошенная шлюха.
- `"BOSS"` → кто-то решил добавить «по приколу», а бизнес-логика к этому не готова.

С enum ты пишешь так:

```java
Role role = Role.COURIER;
```

И получаешь:

- Нельзя написать `Role.KURER` — такой константы нет → компилятор пошлёт.
- IDE подсказывает варианты: `Role.` → видно `COURIER`, `SHOP`, `ADMIN`.
- Везде один источник правды: список ролей определён в **одном месте**.

---

## 3. Наш `Role.java` — как он выглядит

```java
package org.example.flower_delivery.model;

/**
 * Enum (перечисление) ролей пользователей.
 *
 * Роль определяет, что пользователь может делать в системе:
 * - COURIER: курьер, который бегает и доставляет заказы
 * - SHOP: магазин, который создает заказы и следит за доставкой
 * - ADMIN: админ, который рулит всем этим бардаком
 *
 * Enum используем вместо строк, чтобы:
 * - нельзя было опечататься ("COURIER" vs "COURIR")
 * - IDE подсказвала доступные значения
 * - код был типобезопасным (Role, а не просто String)
 */
public enum Role {

    /**
     * Роль: Курьер.
     * Курьер — исполнитель доставки:
     * - видит доступные заказы
     * - может брать заказы
     * - меняет статусы (В магазине, Забрал, В пути, Вручил)
     */
    COURIER,

    /**
     * Роль: Магазин.
     * Магазин:
     * - создает заказы
     * - видит статусы заказов
     * - может видеть, где курьер
     */
    SHOP,

    /**
     * Роль: Администратор.
     * Админ:
     * - активирует/блокирует курьеров и магазины
     * - управляет заказами
     * - настраивает комиссии, штрафы, депозиты
     */
    ADMIN
}
```

---

## 4. Разбор по словам (как ты это усвоил)

### 4.1. `package org.example.flower_delivery.model;`

**Ты сказал:**  
> чтобы прога понимала с какого файла че мы берем.

**Уточнение / поправка:**

- `package` — это не «файл», а **пространство имён / логическая папка**.
- Помогает:
  - отделять слои (`model`, `service`, `repository`),
  - избегать конфликтов имён (`User` в разных пакетах),
  - правильно работать с `import`.

**Запомнить:**  
`package` = «адрес класса в проекте», не просто «имя файла».

---

### 4.2. Комментарии: `//` vs `/** */`

**Ты сказал:**  
> ну у обычного коментария // у док /** */ -  но вопрос хороший чем они отличаются ?

**Ответ:**

- `//` и `/* */` — обычные комментарии, чисто для людей:
  - компилятор их игнорит,
  - IDE может их подсвечивать, но это просто текст.
- `/** ... */` — **Javadoc**:
  - IDE показывает это как документацию при наведении,
  - можно сгенерировать HTML‑доку,
  - это официальное описание класса/метода/поля.

**Пример:**

```java
// просто пометка для себя
int x = 5;

/**
 * Это уже документация к классу/методу.
 */
public class User { ... }
```

---

### 4.3. `public enum Role`

**Ты сказал:**  
> мы делаем его публичным что бы данные могли читаться с любой точки в проекте, так будет проще эти роли взять в репозитории и прочей хуйни.

**Это верно.**

- `public` — тип доступен из любого пакета.
- Нам нужно:
  - использовать `Role` в `User`,
  - использовать `Role` в `UserRepository`,
  - использовать `Role` в `UserService`,
  - проверять роли в хендлерах бота.

Без `public` enum был бы доступен только внутри пакета (package-private).

---

### 4.4. Константы: `COURIER, SHOP, ADMIN`

**Ты сказал про запятые:**  
> потому что там идет перечесление переменных, а после админа нет переменной, вот если мы добавим супер админ поле, то после админ поставим запятую.

**Суть поймал правильно:**

- Enum — это список значений, перечисленных через запятую.
- Между элементами — запятая.
- После последнего — обычно **нет запятой** (по классике Java).

Если добавить `SUPER_ADMIN`, будет:

```java
COURIER,
SHOP,
ADMIN,
SUPER_ADMIN
```

---

## 5. Как `Role` связан с БД и JPA (теория)

### 5.1. В БД — строка, в Java — enum

В миграции V1 мы описали поле:

```sql
role VARCHAR(20) CHECK (role IN ('COURIER', 'SHOP', 'ADMIN'))
```

В Java у нас будет (в модели `User`):

```java
@Enumerated(EnumType.STRING)
@Column(name = "role")
private Role role;
``>

**Как это работает:**

- В БД лежит строка `'COURIER'`.
- Hibernate читает строку, конвертит её в `Role.COURIER`.
- Когда сохраняем:
  - в Java: `user.setRole(Role.ADMIN);`
  - в БД: пишется `'ADMIN'`.

`@Enumerated(EnumType.STRING)` говорит Hibernate:

- «Сохраняй enum как **строку**, а не как номер».

Почему это важно:

- Если бы был `ORDINAL`:
  - `COURIER = 0`, `SHOP = 1`, `ADMIN = 2`,
  - добавляем новый `MANAGER` в середину — все старые данные съезжают.
- Со строками всё явно и читаемо.

---

## 6. Где мы будем использовать `Role` дальше

1. **В `User` модели** — поле `private Role role;`
2. **В `UserRepository`**:
   ```java
   List<User> findByRole(Role role);
   List<User> findByRoleAndIsActiveTrue(Role role);
   ```
3. **В `UserService`**:
   ```java
   public boolean isAdmin(Long telegramId) {
       Optional<Role> roleOpt = getRoleByTelegramId(telegramId);
       return roleOpt.isPresent() && roleOpt.get() == Role.ADMIN;
   }
   ```
4. **В хендлерах бота** — чтобы решать, какие команды/кнопки показывать пользователю.

---

## 7. Контрольные вопросы (для самопроверки)

Попробуй ответить сам (без подсказки), потом загляни в файл и проверь.

1. Зачем нужен `package` в начале файла?  
2. Чем `//` отличается от `/** ... */`? Когда что использовать?
3. Почему `Role` сделан `public`? Что будет, если убрать `public`?
4. Почему enum‑константы пишутся заглавными буквами?
5. Почему после `COURIER` и `SHOP` запятая есть, а после `ADMIN` — нет?
6. Что делает `@Enumerated(EnumType.STRING)`?
7. Какое значение лежит в БД, если в Java `user.getRole() == Role.COURIER`?
8. Чем enum лучше строки `"COURIER"` в коде?

---

## 8. Дальше по плану

Следующий логичный шаг:

1. Модель `User` (связка полей с таблицей `users` через JPA-аннотации).
2. Репозиторий `UserRepository`.
3. Сервис `UserService`.

Но прежде чем туда идти — пробегись по этому файлу, и если хочешь, можешь **в отдельном файле** попробовать своими словами пересказать:

- что такое enum,
- зачем в проекте нужен `Role`,
- как он связан с БД.

Это будет охуенная прокачка памяти. Если что-то будешь пересказывать и застрянешь — скинь текст, разберём и допилим. 💀🚀

