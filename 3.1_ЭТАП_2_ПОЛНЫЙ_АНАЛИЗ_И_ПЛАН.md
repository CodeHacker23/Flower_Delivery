# 🎯 ЭТАП 2: ФУНКЦИОНАЛ МАГАЗИНОВ - ПОЛНЫЙ АНАЛИЗ И ПЛАН

**Дата:** 2026-01-17  
**Статус:** Не начат  
**Стоимость:** 45,000₽  
**Сроки:** 17-31 января (примерно 2 недели)

---

## 📋 СОДЕРЖАНИЕ

1. [Что нужно сделать (кратко)](#что-нужно-сделать)
2. [Детальный разбор каждой задачи](#детальный-разбор)
3. [Архитектура БД для этапа 2](#архитектура-бд)
4. [Что создаем в коде](#что-создаем-в-коде)
5. [Почему именно так (обоснование)](#почему-именно-так)
6. [План реализации по шагам](#план-реализации)
7. [Потенциальные проблемы и решения](#проблемы-и-решения)

---

# ЧТО НУЖНО СДЕЛАТЬ

## 🎯 Три основные задачи:

1. **Регистрация магазина** - магазин регистрируется, указывает адрес забора, админ активирует
2. **Создание заказов** - магазин создает заказы с полной информацией о доставке
3. **Просмотр курьера** - магазин видит, где курьер и может с ним связаться

---

# ДЕТАЛЬНЫЙ РАЗБОР

## 1️⃣ РЕГИСТРАЦИЯ МАГАЗИНА

### Что это такое:

**Магазин** - это пользователь с ролью `SHOP`, который:
- Создает заказы на доставку
- Отслеживает статусы заказов
- Видит, где находится курьер

### Как это работает:

1. **Пользователь пишет боту `/start`** (уже работает из этапа 1)
2. **Бот регистрирует пользователя** в таблице `users` (уже работает)
3. **НОВОЕ:** Пользователь хочет стать магазином → пишет команду `/register_shop`
4. **НОВОЕ:** Бот спрашивает:
   - ФИО (уже есть из `/start`)
   - Телефон (если не указан)
   - Название магазина
   - Адрес забора товаров (где курьер будет забирать заказы)
5. **НОВОЕ:** Бот сохраняет информацию в таблицу `shops`
6. **НОВОЕ:** Админ активирует магазин (меняет `is_active = true` в таблице `users`)

### Зачем это нужно:

- **Разделение ролей:** Магазин - это не просто пользователь, у него есть дополнительная информация (адрес забора, название)
- **Безопасность:** Админ контролирует, кто может создавать заказы
- **Удобство:** Адрес забора указывается один раз, потом используется для всех заказов

### Почему отдельная таблица `shops`:

**Проблема:** Почему не хранить всё в таблице `users`?

**Ответ:** Потому что:
- У магазина есть **дополнительные данные** (название, адрес забора, координаты)
- У курьера будут **другие дополнительные данные** (транспорт, рейтинг)
- У админа **нет дополнительных данных**

**Решение:** Таблица `shops` связана с `users` через `user_id` (FOREIGN KEY)

**Аналогия:** 
- `users` = паспорт (базовая информация: ФИО, телефон)
- `shops` = водительские права (дополнительная информация для магазинов: адрес, название)
- `couriers` (будет в этапе 3) = медицинская справка (дополнительная информация для курьеров)

---

## 2️⃣ СОЗДАНИЕ ЗАКАЗОВ

### Что это такое:

**Заказ** - это запрос на доставку товара от магазина до получателя.

### Как это работает:

1. **Магазин пишет команду `/create_order`** (или нажимает кнопку "Создать заказ")
2. **Бот задает вопросы пошагово:**
   - "Введите номер получателя:" → магазин вводит `+79991234567`
   - "Введите имя получателя:" → магазин вводит `Иван Иванов`
   - "Введите адрес доставки:" → магазин вводит `ул. Ленина, д. 10, кв. 5`
   - "Введите номер заказчика:" → магазин вводит `ORD-12345` (внутренний номер магазина)
   - "Введите интервал доставки (начало):" → магазин вводит `10:00`
   - "Введите интервал доставки (конец):" → магазин вводит `18:00`
   - "Введите цену доставки:" → магазин вводит `500`
   - "Выберите способ оплаты:" → магазин выбирает `Наличные` / `Карта` / `Онлайн`
   - "Введите комментарий (необязательно):" → магазин вводит `Хрупкое, осторожно!`
3. **Бот сохраняет заказ** в таблицу `orders` со статусом `NEW` (новый заказ)
4. **Бот отправляет подтверждение** магазину с информацией о заказе

### Зачем это нужно:

- **Автоматизация:** Магазин создает заказ через бота, не нужно звонить курьеру
- **Отслеживание:** Все заказы хранятся в БД, можно видеть историю
- **Маршрутизация:** Система может автоматически распределять заказы между курьерами (этап 4)

### Почему пошаговый ввод:

**Проблема:** Почему не один большой текст с разделителями?

**Ответ:** Потому что:
- **Валидация:** Можно проверить каждый шаг (телефон - правильный формат, адрес - не пустой)
- **Удобство:** Пользователь видит, что именно нужно ввести
- **Ошибки:** Если ошибка - можно исправить только этот шаг, не переписывать всё
- **Геокодирование:** После ввода адреса можно сразу получить координаты (lat/long)

**Аналогия:** Как заполнение анкеты - по одному вопросу, а не всё сразу.

---

## 3️⃣ ПРОСМОТР КУРЬЕРА

### Что это такое:

**Просмотр курьера** - магазин видит, где находится курьер, который выполняет его заказ.

### Как это работает:

1. **Магазин смотрит список своих заказов** (команда `/my_orders`)
2. **Магазин выбирает заказ** (который уже принят курьером, статус `ACCEPTED` или выше)
3. **Бот показывает:**
   - Информацию о курьере (имя, телефон)
   - Геопозицию курьера на карте (обновляется каждые 30 сек)
   - Кнопку "Позвонить курьеру" (через Telegram Call)
   - Кнопку "Написать курьеру" (через Telegram)

### Зачем это нужно:

- **Прозрачность:** Магазин видит, где курьер, не нужно звонить
- **Планирование:** Магазин знает, когда курьер приедет за заказом
- **Коммуникация:** Быстрая связь с курьером через бота

### Почему обновление каждые 30 сек:

**Проблема:** Почему не каждую секунду?

**Ответ:** Потому что:
- **Нагрузка:** Каждое обновление = запрос к БД + отправка сообщения в Telegram
- **Батарея:** Постоянная отправка геолокации сажает батарею телефона курьера
- **30 сек - оптимально:** Достаточно точно для отслеживания, не перегружает систему

**Аналогия:** Как GPS навигатор - обновляется не каждую секунду, а периодически.

---

# АРХИТЕКТУРА БД

## 📊 Новые таблицы для этапа 2:

### 1. Таблица `shops` (магазины)

**Зачем:** Хранить дополнительную информацию о магазинах (адрес забора, название)

**Структура:**
```sql
CREATE TABLE shops (
    id UUID PRIMARY KEY,                    -- ID магазина
    user_id UUID REFERENCES users(id),      -- Связь с пользователем (FOREIGN KEY)
    name VARCHAR(255) NOT NULL,             -- Название магазина ("Цветочный рай")
    address VARCHAR(500) NOT NULL,          -- Адрес забора ("ул. Ленина, д. 5")
    latitude DOUBLE,                        -- Широта (для геокодирования)
    longitude DOUBLE,                       -- Долгота (для геокодирования)
    phone VARCHAR(50),                      -- Телефон магазина (может отличаться от user.phone)
    is_active BOOLEAN DEFAULT FALSE,        -- Активен ли магазин
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**Почему `user_id` (FOREIGN KEY):**
- Один пользователь = один магазин (1:1 связь)
- Если удалишь пользователя → удалится магазин (CASCADE)
- Можно получить данные пользователя через JOIN

**Почему `latitude` и `longitude`:**
- Для геокодирования адреса (превращаем "ул. Ленина, д. 5" в координаты)
- Для построения маршрутов (этап 4)
- Для проверки, что курьер действительно приехал в магазин (этап 5, защита от обхода)

---

### 2. Таблица `orders` (заказы)

**Зачем:** Хранить все заказы на доставку

**Структура:**
```sql
CREATE TABLE orders (
    id UUID PRIMARY KEY,                                    -- ID заказа
    shop_id UUID REFERENCES shops(id),                      -- Какой магазин создал заказ
    courier_id UUID REFERENCES users(id),                    -- Какой курьер взял заказ (NULL если еще не взят)
    recipient_name VARCHAR(255) NOT NULL,                   -- Имя получателя
    recipient_phone VARCHAR(50) NOT NULL,                  -- Телефон получателя
    delivery_address VARCHAR(500) NOT NULL,                 -- Адрес доставки
    delivery_latitude DOUBLE,                               -- Широта адреса доставки
    delivery_longitude DOUBLE,                              -- Долгота адреса доставки
    order_number VARCHAR(100),                              -- Номер заказа магазина ("ORD-12345")
    delivery_interval_start TIME,                            -- Начало интервала доставки (10:00)
    delivery_interval_end TIME,                              -- Конец интервала доставки (18:00)
    price DECIMAL(10, 2) NOT NULL,                          -- Цена доставки (500.00)
    payment_method VARCHAR(20) CHECK (payment_method IN ('CASH', 'CARD', 'ONLINE')),  -- Способ оплаты
    comment TEXT,                                            -- Комментарий к заказу
    status VARCHAR(20) CHECK (status IN ('NEW', 'ACCEPTED', 'IN_SHOP', 'PICKED_UP', 'ON_WAY', 'DELIVERED', 'RETURNED', 'CANCELLED')),  -- Статус заказа
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**Почему `shop_id` (FOREIGN KEY):**
- Один магазин может создать много заказов (1:М связь)
- Если удалишь магазин → можно удалить все его заказы (или оставить для истории)

**Почему `courier_id` может быть NULL:**
- Заказ создан, но еще не взят курьером → `courier_id = NULL`
- Курьер взял заказ → `courier_id = UUID курьера`

**Почему `status` (enum через CHECK):**
- Отслеживание жизненного цикла заказа
- Фильтрация заказов по статусам
- История изменений (этап 4, таблица `order_status_history`)

**Статусы заказа:**
- `NEW` - новый заказ, еще не взят курьером
- `ACCEPTED` - курьер принял заказ
- `IN_SHOP` - курьер приехал в магазин
- `PICKED_UP` - курьер забрал заказ из магазина
- `ON_WAY` - курьер в пути к получателю
- `DELIVERED` - заказ доставлен получателю
- `RETURNED` - заказ возвращен в магазин
- `CANCELLED` - заказ отменен

**Почему `delivery_latitude` и `delivery_longitude`:**
- Для геокодирования адреса доставки
- Для построения маршрутов (этап 4)
- Для проверки, что курьер действительно приехал к получателю (этап 5)

**Почему `delivery_interval_start` и `delivery_interval_end` (TIME, не TIMESTAMP):**
- Интервал доставки - это время суток (10:00-18:00), а не конкретная дата
- Можно использовать один и тот же интервал для разных дней

**Почему `price` (DECIMAL, не DOUBLE):**
- Деньги должны быть точными (500.00), а не приблизительными (500.0000001)
- DECIMAL хранит точные значения, DOUBLE - приблизительные (плавающая точка)

---

### 3. Таблица `courier_locations` (геолокация курьеров) - для этапа 2

**Зачем:** Хранить текущую позицию курьера для отслеживания магазином

**Структура:**
```sql
CREATE TABLE courier_locations (
    id UUID PRIMARY KEY,
    courier_id UUID REFERENCES users(id),   -- Какой курьер
    latitude DOUBLE NOT NULL,                -- Широта
    longitude DOUBLE NOT NULL,                -- Долгота
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,  -- Когда обновлена позиция
    order_id UUID REFERENCES orders(id)       -- Для какого заказа (может быть NULL если курьер просто едет)
);
```

**Почему отдельная таблица:**
- Геолокация обновляется часто (каждые 30 сек)
- История перемещений (можно посмотреть, где был курьер)
- Не засоряем таблицу `users` частыми обновлениями

**Почему `order_id` может быть NULL:**
- Курьер может просто ехать домой (не на заказ)
- Или курьер еще не взял заказ, но уже включил геолокацию

**Оптимизация (для этапа 4):**
- Можно хранить только последнюю позицию каждого курьера (удалять старые)
- Или хранить историю для аналитики

---

# ЧТО СОЗДАЕМ В КОДЕ

## 📁 Структура файлов:

```
src/main/java/org/example/flower_delivery/
├── model/
│   ├── User.java                    ✅ (уже есть, этап 1)
│   ├── Role.java                    ✅ (уже есть, этап 1)
│   ├── Shop.java                    🆕 (НОВЫЙ - модель магазина)
│   ├── Order.java                   🆕 (НОВЫЙ - модель заказа)
│   ├── OrderStatus.java             🆕 (НОВЫЙ - enum статусов заказа)
│   ├── PaymentMethod.java           🆕 (НОВЫЙ - enum способов оплаты)
│   └── CourierLocation.java         🆕 (НОВЫЙ - модель геолокации курьера)
│
├── repository/
│   ├── UserRepository.java          ✅ (уже есть, этап 1)
│   ├── ShopRepository.java          🆕 (НОВЫЙ - репозиторий магазинов)
│   ├── OrderRepository.java         🆕 (НОВЫЙ - репозиторий заказов)
│   └── CourierLocationRepository.java 🆕 (НОВЫЙ - репозиторий геолокации)
│
├── service/
│   ├── UserService.java             ✅ (уже есть, этап 1)
│   ├── ShopService.java             🆕 (НОВЫЙ - бизнес-логика магазинов)
│   ├── OrderService.java            🆕 (НОВЫЙ - бизнес-логика заказов)
│   └── GeocodingService.java        🆕 (НОВЫЙ - геокодирование адресов)
│
├── handler/
│   ├── StartCommandHandler.java     ✅ (уже есть, этап 1)
│   ├── ShopRegistrationHandler.java 🆕 (НОВЫЙ - регистрация магазина)
│   ├── OrderCreationHandler.java    🆕 (НОВЫЙ - создание заказа)
│   ├── OrderListHandler.java        🆕 (НОВЫЙ - список заказов магазина)
│   └── CourierTrackingHandler.java  🆕 (НОВЫЙ - отслеживание курьера)
│
└── util/
    └── TelegramKeyboardBuilder.java 🆕 (НОВЫЙ - утилита для создания клавиатур)
```

---

## 🗄️ Миграции БД:

```
src/main/resources/db/migration/
├── V1__create_users_table.sql       ✅ (уже есть, этап 1)
├── V2__create_shops_table.sql       🆕 (НОВЫЙ - таблица shops)
├── V3__create_orders_table.sql      🆕 (НОВЫЙ - таблица orders)
└── V4__create_courier_locations_table.sql 🆕 (НОВЫЙ - таблица courier_locations)
```

---

# ПОЧЕМУ ИМЕННО ТАК

## 🎯 Архитектурные решения:

### 1. Почему отдельные таблицы `shops` и `orders`?

**Проблема:** Можно было бы хранить всё в одной таблице `users`.

**Ответ:** 
- **Нормализация БД:** Каждая таблица отвечает за свою сущность
- **Масштабируемость:** Легко добавлять новые поля (например, рейтинг магазина)
- **Производительность:** Индексы работают быстрее на отдельных таблицах
- **Гибкость:** Можно иметь несколько магазинов у одного пользователя (в будущем)

**Аналогия:** Как папки в компьютере - лучше разделить по категориям, чем всё в одной куче.

---

### 2. Почему пошаговый ввод заказа?

**Проблема:** Можно было бы один большой текст с разделителями.

**Ответ:**
- **Валидация:** Проверяем каждый шаг (телефон - правильный формат)
- **UX:** Пользователь видит, что именно нужно ввести
- **Геокодирование:** После ввода адреса сразу получаем координаты
- **Ошибки:** Можно исправить только один шаг, не переписывать всё

**Аналогия:** Как заполнение анкеты - по одному вопросу, а не всё сразу.

---

### 3. Почему геолокация в отдельной таблице?

**Проблема:** Можно было бы хранить в таблице `users` или `orders`.

**Ответ:**
- **Частота обновлений:** Геолокация обновляется каждые 30 сек, не нужно обновлять всю таблицу `users`
- **История:** Можно хранить историю перемещений для аналитики
- **Производительность:** Меньше нагрузка на основную таблицу

**Аналогия:** Как лог-файл - отдельный файл для частых записей, не засоряем основной файл.

---

# ПЛАН РЕАЛИЗАЦИИ

## 📅 Пошаговый план (примерно 2 недели):

### Неделя 1: База данных и модели

#### День 1-2: Миграции БД
- [ ] Создать миграцию `V2__create_shops_table.sql`
- [ ] Создать миграцию `V3__create_orders_table.sql`
- [ ] Создать миграцию `V4__create_courier_locations_table.sql`
- [ ] Протестировать миграции (запустить приложение, проверить в pgAdmin)

#### День 3-4: Модели Java
- [ ] Создать enum `OrderStatus` (NEW, ACCEPTED, IN_SHOP, ...)
- [ ] Создать enum `PaymentMethod` (CASH, CARD, ONLINE)
- [ ] Создать модель `Shop.java` с JPA аннотациями
- [ ] Создать модель `Order.java` с JPA аннотациями
- [ ] Создать модель `CourierLocation.java` с JPA аннотациями

#### День 5: Репозитории
- [ ] Создать `ShopRepository.java` (методы: findByUserId, findByIsActiveTrue)
- [ ] Создать `OrderRepository.java` (методы: findByShopId, findByStatus, findByCourierId)
- [ ] Создать `CourierLocationRepository.java` (методы: findByCourierId, findLatestByCourierId)

---

### Неделя 2: Сервисы и обработчики

#### День 6-7: Сервисы
- [ ] Создать `ShopService.java` (регистрация магазина, активация админом)
- [ ] Создать `OrderService.java` (создание заказа, изменение статуса, фильтрация)
- [ ] Создать `GeocodingService.java` (геокодирование адресов через API 2GIS/Yandex)

#### День 8-9: Обработчики команд
- [ ] Создать `ShopRegistrationHandler.java` (команда `/register_shop`, пошаговый ввод)
- [ ] Создать `OrderCreationHandler.java` (команда `/create_order`, пошаговый ввод)
- [ ] Создать `OrderListHandler.java` (команда `/my_orders`, список заказов с фильтрами)
- [ ] Создать `CourierTrackingHandler.java` (просмотр геопозиции курьера)

#### День 10: Интеграция и тестирование
- [ ] Подключить обработчики к `Bot.java`
- [ ] Протестировать регистрацию магазина
- [ ] Протестировать создание заказа
- [ ] Протестировать список заказов
- [ ] Исправить баги

---

# ПРОБЛЕМЫ И РЕШЕНИЯ

## ⚠️ Потенциальные проблемы:

### 1. Геокодирование адресов

**Проблема:** Как превратить "ул. Ленина, д. 10" в координаты (latitude, longitude)?

**Решение:** Использовать API геокодирования:
- **2GIS API** (бесплатно до 1000 запросов/день)
- **Yandex Geocoding API** (бесплатно до 10000 запросов/день)
- **Google Geocoding API** (платно, но точнее)

**Реализация:** Создать `GeocodingService.java`, который:
1. Принимает адрес (строка)
2. Отправляет запрос к API
3. Получает координаты (lat, long)
4. Сохраняет в БД

**Аналогия:** Как GPS навигатор - вводишь адрес, получаешь координаты.

---

### 2. Пошаговый ввод (State Machine)

**Проблема:** Как запомнить, на каком шаге находится пользователь?

**Решение:** Использовать Redis или таблицу `user_sessions`:
- Хранить текущее состояние пользователя (какой шаг, какие данные уже введены)
- При следующем сообщении - продолжать с того же места

**Реализация (для этапа 2, без Redis):**
- Таблица `user_sessions`:
  ```sql
  CREATE TABLE user_sessions (
      user_id UUID REFERENCES users(id),
      current_step VARCHAR(50),  -- "ORDER_RECIPIENT_PHONE", "ORDER_RECIPIENT_NAME", ...
      session_data JSONB,        -- Временные данные (что уже введено)
      expires_at TIMESTAMP        -- Когда истекает сессия
  );
  ```

**Аналогия:** Как заполнение многостраничной формы - сохраняем прогресс.

---

### 3. Обновление геолокации каждые 30 сек

**Проблема:** Как заставить курьера отправлять геолокацию каждые 30 сек?

**Решение (для этапа 2, упрощенное):**
- Курьер вручную отправляет геолокацию через команду `/update_location`
- Или курьер нажимает кнопку "Обновить позицию"

**Решение (для этапа 4, продвинутое):**
- Бот автоматически запрашивает геолокацию каждые 30 сек (через `RequestLocation`)
- Или курьер включает "Режим отслеживания" - бот автоматически запрашивает геолокацию

**Аналогия:** Как "Поделиться геолокацией" в WhatsApp - включаешь один раз, обновляется автоматически.

---

### 4. Валидация данных

**Проблема:** Как проверить, что телефон правильный, адрес существует?

**Решение:**
- **Телефон:** Регулярное выражение (regex) для проверки формата
- **Адрес:** Геокодирование - если API не нашел адрес, значит неправильный
- **Цена:** Проверка, что число > 0
- **Интервал:** Проверка, что начало < конца

**Реализация:** В `OrderService.createOrder()` - валидация перед сохранением.

---

### 5. Производительность при большом количестве заказов

**Проблема:** Если у магазина 1000 заказов, как быстро показать список?

**Решение:**
- **Пагинация:** Показывать по 10-20 заказов на страницу
- **Индексы:** Создать индекс на `shop_id` и `status` в таблице `orders`
- **Фильтрация:** Показывать только активные заказы (NEW, ACCEPTED, IN_SHOP)

**Реализация:** В `OrderRepository` - методы с `Pageable` (Spring Data JPA).

---

# ИТОГОВЫЙ ЧЕКЛИСТ ЭТАПА 2

## ✅ Что должно работать после этапа 2:

1. ✅ Магазин может зарегистрироваться (`/register_shop`)
2. ✅ Магазин может создать заказ (`/create_order`)
3. ✅ Магазин может посмотреть список своих заказов (`/my_orders`)
4. ✅ Магазин может посмотреть геопозицию курьера (если заказ взят)
5. ✅ Админ может активировать магазин (через БД или команду `/activate_shop`)
6. ✅ Геокодирование адресов работает (адрес → координаты)

---

# СЛЕДУЮЩИЕ ШАГИ

После завершения этапа 2:
- **Этап 3:** Функционал курьеров (курьер может брать заказы, менять статусы)
- **Этап 4:** Продвинутые функции (автоматическая маршрутизация, геолокация в реальном времени)

---

**Готов начинать этап 2? Начинаем с миграций БД!** 🚀
