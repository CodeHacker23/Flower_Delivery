# 🔄 КАК СИНХРОНИЗИРОВАТЬ GRADLE В IDEA

## 🎯 Проблема:
После изменения `build.gradle` нужно синхронизировать зависимости, чтобы IDEA подтянула новые библиотеки.

---

## ✅ СПОСОБ 1: Правой кнопкой на build.gradle (САМЫЙ ПРОСТОЙ!)

### Что делать:
1. **Найди файл `build.gradle`** в корне проекта (в левой панели "Project")
   - Не в `src`!
   - Не в `gradle`!
   - **Именно в корне проекта: `Flower_Delivery/build.gradle`**

2. **Правой кнопкой на `build.gradle`**

3. В контекстном меню найди **"Gradle"** → **"Reload Gradle Project"**
   - Или просто **"Reload Gradle Project"** (если напрямую в меню)

---

## ✅ СПОСОБ 2: Через панель Gradle (ЕСЛИ НЕ ВИДНО МЕНЮ)

### Что делать:
1. **Открой панель Gradle:**
   - **View** → **Tool Windows** → **Gradle**
   - Или справа в IDEA нажми иконку **Gradle** (если видна)

2. В панели Gradle найди кнопку **"Reload All Gradle Projects"**
   - Иконка: две стрелки по кругу (🔄)
   - Обычно вверху панели

3. **Нажми на неё**

---

## ✅ СПОСОБ 3: Через уведомление IDEA

### Что делать:
1. **Открой файл `build.gradle`** (двойной клик)

2. **Посмотри вверху редактора** - IDEA может показать желтую полоску:
   ```
   ⚠️ Gradle files have changed
   [Reload project] [Dismiss]
   ```

3. **Нажми "Reload project"**

---

## ✅ СПОСОБ 4: Через кнопку слона (Gradle иконка)

### Что делать:
1. **Найди иконку слона** (🐘) справа вверху IDEA (рядом с другими иконками инструментов)

2. **Кликни на неё** - откроется панель Gradle

3. **Найди кнопку "Reload All Gradle Projects"** и нажми

---

## 🔍 КАК ПОНЯТЬ, ЧТО СИНХРОНИЗАЦИЯ ПРОШЛА:

### ✅ УСПЕШНО:
- Внизу IDEA появится индикатор: **"Gradle build finished"** или **"Gradle sync finished"**
- Ошибки в `build.gradle` исчезнут (если были)
- В панели Gradle появится дерево задач (Tasks)

### ❌ ОШИБКА:
- Внизу появится красная полоска с ошибкой
- В консоли (Run/Debug) будет текст ошибки
- В панели Gradle будет красный крестик

---

## 🚨 ЕСЛИ НЕ ВИДНО ОПЦИЮ "Gradle" В КОНТЕКСТНОМ МЕНЮ:

1. **Убедись, что кликаешь именно на `build.gradle`**, а не на `src` или другую папку
2. Попробуй **Способ 2** (через панель Gradle)
3. Или **Способ 3** (через уведомление при открытии файла)

---

## 💡 СОВЕТ:

**После синхронизации Gradle:**
1. Дождись завершения (обычно 10-30 секунд)
2. Попробуй запустить проект: **Run** → **Run 'FlowerDeliveryApplication'**
3. Если ошибок нет - всё ок!

---

**Попробуй Способ 1 или Способ 2 - они самые надежные!** 🚀
