#0.2_🔐 КАК СБРОСИТЬ ПАРОЛЬ БД (PostgreSQL или MySQL)

## 🐘 ПОСТГРЕС (PostgreSQL) - если используешь его

### Способ 1: Через pgAdmin (САМЫЙ ПРОСТОЙ!)

1. **Открой pgAdmin** (он у тебя уже открыт)
2. **Подключись к серверу** (может быть уже подключен)
3. **Правой кнопкой на сервере** → **Properties** → вкладка **Connection**
4. Если там есть сохраненный пароль - можешь его посмотреть (или сбросить)
5. Если нужно **ИЗМЕНИТЬ пароль**:
   - **Правой кнопкой на сервере** → **Properties** → вкладка **Connection**
   - Введи **НОВЫЙ пароль** в поле Password
   - Или через SQL: `ALTER USER postgres PASSWORD 'новый_пароль';`

### Способ 2: Сброс через файл конфигурации (если совсем забыл)

**ВНИМАНИЕ: Это немного сложнее, но работает 100%!**

1. **Останови PostgreSQL** (если запущен как сервис):
   - Win+R → `services.msc` → найди "postgresql" → Stop

2. **Найди файл `pg_hba.conf`**:
   - Обычно здесь: `C:\Program Files\PostgreSQL\<версия>\data\pg_hba.conf`
   - Или поищи через Проводник

3. **Открой `pg_hba.conf` в блокноте (как администратор!)**

4. **Найди строку:**
   ```
   host    all             all             127.0.0.1/32            scram-sha-256
   ```
   Или похожую с `md5` или `password`

5. **Замени на:**
   ```
   host    all             all             127.0.0.1/32            trust
   ```
   (Меняем `scram-sha-256` или `md5` на `trust` - это отключает проверку пароля локально)

6. **Сохрани файл**

7. **Запусти PostgreSQL** (через services.msc или через pgAdmin)

8. **Подключись через pgAdmin БЕЗ пароля** (или через командную строку):
   ```bash
   psql -U postgres
   ```

9. **Смени пароль:**
   ```sql
   ALTER USER postgres PASSWORD 'твой_новый_пароль';
   ```

10. **Верни `pg_hba.conf` обратно** (замени `trust` обратно на `scram-sha-256` или `md5`)

11. **Перезапусти PostgreSQL**

12. **Попробуй подключиться с новым паролем!**

---

## 🐬 MySQL - если используешь его

### Способ 1: Через MySQL Workbench

Если у тебя установлен MySQL Workbench - там могут быть сохраненные пароли в настройках.

### Способ 2: Сброс через безопасный режим

1. **Останови MySQL**:
   - Win+R → `services.msc` → найди "MySQL" → Stop

2. **Открой командную строку как администратор** (Win+X → "Windows PowerShell (Admin)")

3. **Перейди в папку MySQL**:
   ```cmd
   cd "C:\Program Files\MySQL\MySQL Server <версия>\bin"
   ```
   (Замени `<версия>` на свою версию, например `8.0`)

4. **Запусти MySQL в безопасном режиме** (без пароля):
   ```cmd
   mysqld --skip-grant-tables --console
   ```
   Оставь это окно открытым!

5. **Открой ВТОРОЕ окно командной строки** (тоже как администратор)

6. **Подключись к MySQL БЕЗ пароля**:
   ```cmd
   mysql -u root
   ```

7. **Сбрось пароль:**
   ```sql
   USE mysql;
   UPDATE user SET authentication_string=PASSWORD('новый_пароль') WHERE User='root';
   FLUSH PRIVILEGES;
   EXIT;
   ```
   
   Для MySQL 8.0+ вместо `PASSWORD()` используй:
   ```sql
   USE mysql;
   ALTER USER 'root'@'localhost' IDENTIFIED BY 'новый_пароль';
   FLUSH PRIVILEGES;
   EXIT;
   ```

8. **Закрой первое окно** (где `mysqld --skip-grant-tables`)

9. **Запусти MySQL нормально** (через services.msc)

10. **Подключись с новым паролем!**

---

## 🎯 МОЙ СОВЕТ

**Если у тебя уже установлен PostgreSQL и он работает:**

1. **Попробуй стандартные пароли**:
   - `postgres` (часто по умолчанию)
   - `admin`
   - `root`
   - Пустой пароль (просто нажми Enter)

2. **Если ничего не работает** - используй Способ 2 для PostgreSQL (через `pg_hba.conf`)

3. **Или создай нового пользователя** через pgAdmin (если можешь подключиться как другой пользователь)

---

## ❓ ВАЖНЫЙ ВОПРОС

**Какая БД у тебя установлена - PostgreSQL или MySQL?**

- Если **PostgreSQL** (как на скриншотах) - используем его, переделаю конфиг
- Если **MySQL** - используем MySQL, используем инструкцию выше
- Если **ОБЕ** - выбирай какую удобнее (я рекомендую PostgreSQL, если он уже работает)

**Напиши какая БД, и я помогу настроить проект под неё!** 🚀
