# 🏪 МОДЕЛЬ SHOP — ПОЛНЫЙ ОБУЧАЮЩИЙ ГАЙД

**Тема:** JPA-сущность `Shop`, маппинг на таблицу `shops`.  
**Файл модели:** `src/main/java/org/example/flower_delivery/model/Shop.java`  
**Миграция:** `V2__create_shops_table.sql`

Разбираем каждую аннотацию, каждое поле и все «магические числа» (255, 500, 50, 10, 8 и т.д.).

---

## 📋 СОДЕРЖАНИЕ

1. [Зачем вообще отдельная сущность Shop](#зачем-отдельная-сущность-shop)
2. [Разбор аннотаций класса](#разбор-аннотаций-класса)
3. [Разбор полей по строчкам](#разбор-полей-по-строчкам)
4. [Почему 255, 500, 50 — откуда длины](#почему-255-500-50)
5. [BigDecimal, precision, scale](#bigdecimal-precision-scale)
6. [@OneToOne и @JoinColumn](#onetoone-и-joincolumn)
7. [FetchType.LAZY](#fetchtype-lazy)
8. [Твой код — что проверить](#твой-код-что-проверить)

---

# ЗАЧЕМ ОТДЕЛЬНАЯ СУЩНОСТЬ SHOP

**В БД уже есть `users`.** Зачем ещё таблица `shops`?

- В `users` храним общее: `telegram_id`, `full_name`, `phone`, `role`, `is_active`.
- У **магазина** есть своё: название, адрес забора, координаты, флаг «магазин активен».
- У **курьера** потом будет своё: транспорт, рейтинг и т.д.
- У **админа** доп. полей нет.

Один пользователь = одна роль. Но доп. данные по ролям разные → выносим в отдельные таблицы: `shops`, потом `couriers` и т.д.

**Связь:** один пользователь — один магазин (1:1). В БД это `shops.user_id` → `users.id`, причём `user_id` UNIQUE.

---

# РАЗБОР АННОТАЦИЙ КЛАССА

Они стоят над объявлением класса `Shop`.

```java
@Entity
@Table(name = "shops")
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class Shop {
```

## @Entity

- **Пакет:** `jakarta.persistence.*`
- **Смысл:** этот класс — описание строки таблицы. Hibernate по нему строит маппинг и SQL.
- **Обязательно:** у сущности должен быть поле с `@Id`.

## @Table(name = "shops")

- **Параметр `name`:** имя таблицы в БД.
- **Почему именно `"shops"`:** в миграции создаётся таблица `shops`. Регистр и имя должны совпадать с тем, что в SQL (в PostgreSQL без кавычек имена обычно lowercase).
- **Если не указать:** по умолчанию JPA возьмёт имя класса в нижнем регистре — `shop`. У нас таблица `shops` → указываем явно.

## @Getter / @Setter

- **Пакет:** Lombok.
- **@Getter** — генерирует методы вида `getId()`, `getShopName()`, `getUser()` и т.д.
- **@Setter** — генерирует `setId()`, `setShopName()` и т.д.
- Зачем: не писать десятки геттеров/сеттеров вручную; Hibernate и сервисы пользуются именно ими.

## @NoArgsConstructor

- Конструктор без аргументов: `public Shop() {}`.
- Нужен Hibernate при загрузке сущности из БД (создаёт объект через рефлексию и потом выставляет поля).

## @AllArgsConstructor

- Конструктор со всеми полями по порядку.
- Удобно для тестов и для других конструкторов/билдера.

## @Builder

- Даёт цепочку вида:  
  `Shop.builder().shopName("...").pickupAddress("...").user(user).build()`.
- Удобно создавать объект, не заполняя лишние поля.

---

# РАЗБОР ПОЛЕЙ ПО СТРОЧКАМ

Разбираем по блокам: что за поле, зачем такая аннотация и такие параметры.

---

## 1. Поле `id`

```java
@Id
@GeneratedValue
@Column(name = "id", nullable = false, updatable = false)
private UUID id;
```

- **@Id** — первичный ключ таблицы.
- **@GeneratedValue** — значение не задаёшь вручную, его «генерируют».  
  У нас в миграции: `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`.  
  Чтобы в Java тоже не путаться с типом, лучше писать явно:  
  `@GeneratedValue(strategy = GenerationType.UUID)` — тогда и Java, и БД работают с UUID единообразно. В гайде ниже есть уточнение.
- **@Column(name = "id", nullable = false, updatable = false):**
  - `name = "id"` — колонка в БД тоже `id`;
  - `nullable = false` — NOT NULL;
  - `updatable = false` — после записи в БД это поле меняться не должно.
- **Тип `UUID`** — соответствует `UUID` в PostgreSQL.

---

## 2. Поле `user` (связь с User)

```java
@OneToOne(fetch = FetchType.LAZY)
@JoinColumn(
        name = "user_id",
        nullable = false,
        unique = true
)
private User user;
```

- **@OneToOne** — связь «один к одному»: один `Shop` — один `User`, один `User` — один `Shop`. В БД это колонка `user_id` в `shops` с UNIQUE.
- **@JoinColumn** — эта колонка «живёт» в таблице `shops`:
  - `name = "user_id"` — имя колонки в БД (FOREIGN KEY на `users.id`);
  - `nullable = false` — NOT NULL;
  - `unique = true` — UNIQUE, т.е. один пользователь — один магазин.
- **Тип `User`** — вторая сторона связи; Hibernate подставляет `users.id` по `shops.user_id`.
- **fetch = FetchType.LAZY** — отдельно разобрано ниже.

---

## 3. Поле `shopName`

```java
@Column(name = "shop_name", nullable = false, length = 255)
private String shopName;
```

- **name = "shop_name"** — в миграции колонка называется `shop_name` (snake_case в БД, camelCase в Java).
- **nullable = false** — NOT NULL.
- **length = 255** — в БД это `VARCHAR(255)`. Откуда 255 — см. раздел «Почему 255, 500, 50».

---

## 4. Поле `pickupAddress`

```java
@Column(name = "pickup_address", nullable = false, length = 500)
private String pickupAddress;
```

- **name = "pickup_address"** — колонка `pickup_address` в БД.
- **nullable = false** — адрес забора обязателен.
- **length = 500** — в миграции `VARCHAR(500)`. Адрес может быть длинным («ул. …, д. …, корп. …, офис …, вход с …»), поэтому 500, а не 255.

---

## 5. Поля `latitude` и `longitude`

```java
@Column(name = "latitude", precision = 10, scale = 8)
private BigDecimal latitude;

@Column(name = "longitude", precision = 11, scale = 8)
private BigDecimal longitude;
```

- **Тип `BigDecimal`** — для десятичных чисел без потери точности (координаты, деньги). С `double` были бы округления.
- **precision** — сколько всего значащих цифр (вместе до и после запятой).
- **scale** — сколько из них после запятой.
- В миграции:
  - `latitude DECIMAL(10, 8)` → `precision = 10`, `scale = 8`;
  - `longitude DECIMAL(11, 8)` → `precision = 11`, `scale = 8`.
- Подробнее про 10/8 и 11/8 — в разделе «BigDecimal, precision, scale».

---

## 6. Поле `phone`

```java
@Column(name = "phone", length = 50)
private String phone;
```

- **nullable** не указан — по умолчанию можно NULL, в миграции `phone VARCHAR(50)` без NOT NULL — так и задумано.
- **length = 50** — чтобы влез любой разумный формат («+7 (999) 123-45-67», «8 999 123 45 67» и т.п.). Откуда 50 — в разделе «Почему 255, 500, 50».

---

## 7. Поле `isActive`

```java
@Column(name = "is_active", nullable = false)
@Builder.Default
private Boolean isActive = false;
```

- **nullable = false** — в БД `is_active BOOLEAN ... NOT NULL`.
- **@Builder.Default** — при вызове `Shop.builder().shopName("...").build()` поле `isActive` будет `false`, а не `null`. Без `@Builder.Default` Builder мог бы проставить `null`.
- **Boolean, не boolean** — чтобы можно было различать «ещё не задано» и «явно false» там, где нужно; для флага «активен/неактивен» по сути везде задано.

---

## 8. Поля `createdAt` и `updatedAt`

```java
@CreationTimestamp
@Column(name = "created_at", nullable = false, updatable = false)
private LocalDateTime createdAt;

@UpdateTimestamp
@Column(name = "updated_at", nullable = false)
private LocalDateTime updatedAt;
```

- **@CreationTimestamp** (Hibernate) — при первом `persist` в поле подставляется текущее время. Соответствует `DEFAULT CURRENT_TIMESTAMP` в миграции для `created_at`.
- **@UpdateTimestamp** — при каждом изменении сущности и сохранении поле обновляется. Аналог «updated_at обновлять при UPDATE».
- **updatable = false** у `created_at` — менять «дату создания» вручную через JPA не даём.
- **LocalDateTime** — дата/время без часового пояса; для «логического» времени создания/обновления в одной системе этого достаточно.

---

# ПОЧЕМУ 255, 500, 50 — ОТКУДА ДЛИНЫ

Это не случайные числа, а сознательный выбор под тип данных и стандарты.

## length = 255 (`shop_name`)

- В миграции: `shop_name VARCHAR(255) NOT NULL`.
- Исторически 255 — распространённый лимит для «короткого текста» (названия, имена, заголовки).
- Эмодзи и кириллица в UTF-8 могут занимать несколько байт на символ, но ограничение задаётся в **символах**, а не в байтах — 255 символов для названия магазина с запасом.
- Итог: 255 — компромисс «не раздувать таблицу» и «хватить на любое разумное название».

## length = 500 (`pickup_address`)

- В миграции: `pickup_address VARCHAR(500) NOT NULL`.
- Адрес может быть длинным: улица, дом, корпус, строение, офис, подъезд, код, «вход со двора» и т.д.
- 255 для такого текста часто мало; 500 — типичный выбор для одного адресного поля в таких системах.
- Итог: 500 берётся под «одно длинное адресное поле».

## length = 50 (`phone`)

- В миграции: `phone VARCHAR(50)`.
- Нужно вместить формат с пробелами, скобками, плюсом: `+7 (999) 123-45-67`, и запас на доп. цифры (городской, добавочный и т.п.).
- 50 символов для одного телефона с большим запасом.
- Итог: 50 — практичный лимит под один номер.

Если бы захотели хранить «просто цифры», можно было бы 20–30, но 50 не мешает и оставляет запас под любые форматы.

---

# BIGDECIMAL, PRECISION, SCALE

## Зачем BigDecimal для координат

- `double`/`float` — числа с плавающей точкой, возможны ошибки округления.
- Координаты используются для расчёта расстояний, зон, маршрутов — там нужна предсказуемая точность.
- В БД мы храним `DECIMAL(10,8)` / `DECIMAL(11,8)` — «фиксированная точка». В Java этому соответствует `BigDecimal`.

## precision и scale

- **precision** — сколько всего цифр (знаков) в числе.
- **scale** — сколько из них после запятой.

Примеры:

- `DECIMAL(10, 8)`:
  - всего 10 цифр, 8 после запятой → 2 цифры до запятой;
  - диапазон примерно от `-9.99999999` до `99.99999999`;
  - для широты (-90 … +90) и долготы в разумных границах этого хватает.
- `DECIMAL(11, 8)`:
  - 11 цифр, 8 после запятой → 3 до запятой;
  - подходит для долготы от -180 до +180 с высокой точностью.

В `@Column` мы просто повторяем те же числа, что и в миграции:

- `latitude`  → `precision = 10`, `scale = 8`;
- `longitude` → `precision = 11`, `scale = 8`.

Так JPA и БД говорят на одном языке по точности.

---

# @ONETOONE И @JOINCOLUMN

## @OneToOne

- Один `Shop` — один `User`, один `User` — один `Shop`.
- В БД это колонка `user_id` в таблице `shops` (у нас «владеющая» сторона — Shop, там и лежит внешний ключ).

Отличие от @ManyToOne:

- **@ManyToOne** — «много сущностей к одному» (например, много заказов к одному магазину).
- **@OneToOne** — «одна к одной» (один пользователь — один магазин).

## @JoinColumn

- Указывает: в **текущей** таблице (`shops`) будет колонка внешнего ключа.
- **name = "user_id"** — имя этой колонки.
- **nullable = false** — магазин не может существовать без пользователя.
- **unique = true** — один и тот же `user_id` не может встретиться в двух записях `shops`, то есть один пользователь — один магазин.

Итог: `@OneToOne` + `@JoinColumn(..., unique = true)` и дают связь 1:1 в сторону User.

---

# FETCHTYPE.LAZY

```java
@OneToOne(fetch = FetchType.LAZY)
```

- **LAZY** — сущность `User` подгружается не в момент загрузки `Shop`, а в момент первого обращения к `shop.getUser()` (если сессия Hibernate ещё открыта).
- **EAGER** (по умолчанию у @OneToOne) — пользователь подтягивается сразу при загрузке магазина (доп. запрос или join).

Зачем LAZY для `user` в Shop:

- Часто нам нужен только магазин: название, адрес, активность. User не трогаем → лишний запрос не делаем.
- Когда реально нужен пользователь (например, `shop.getUser().getTelegramId()`), тогда Hibernate сделает запрос к `users` по `user_id`.

Итог: меньше лишних запросов и загрузок — быстрее и понятнее поведение.

---

# ТВОЙ КОД — ЧТО ПРОВЕРИТЬ

По твоему текущему `Shop.java`:

1. **@GeneratedValue**  
   Сейчас стоит просто `@GeneratedValue`. Для единообразия с `User` и явной работы с UUID лучше писать:
   ```java
   @GeneratedValue(strategy = GenerationType.UUID)
   ```
   Тогда и в коде, и в БД везде фигурирует один и тот же тип генерации.

2. **Остальное**  
   Имена полей и колонок, типы (`UUID`, `BigDecimal`, `LocalDateTime`), `nullable`/`unique`/`length`/`precision`/`scale` соответствуют миграции V2. Структура и аннотации выглядят правильно.

Дальше по плану: репозиторий `ShopRepository`, потом `ShopService`, затем обработчик регистрации магазина.

---

## Краткая шпаргалка по полям

| Поле в Java   | Колонка в БД   | Тип в БД       | Длина/precision/scale | Зачем такое значение        |
|---------------|----------------|----------------|------------------------|-----------------------------|
| id            | id             | UUID           | —                      | PK, как в users            |
| user          | user_id        | UUID (FK)      | —                      | 1:1 с users, UNIQUE        |
| shopName      | shop_name      | VARCHAR        | 255                    | «Короткий текст», стандарт  |
| pickupAddress | pickup_address | VARCHAR        | 500                    | Длинный адрес               |
| latitude      | latitude       | DECIMAL        | 10, 8                  | Широта, точность до метра   |
| longitude     | longitude      | DECIMAL        | 11, 8                  | Долгота, тот же смысл       |
| phone         | phone          | VARCHAR        | 50                     | Один номер с любым форматом |
| isActive      | is_active      | BOOLEAN        | —                      | Флаг активности            |
| createdAt     | created_at     | TIMESTAMP      | —                      | Время создания             |
| updatedAt     | updated_at     | TIMESTAMP      | —                      | Время последнего обновления |

Если хочешь, в следующем шаге можем точечно поправить только `@GeneratedValue(strategy = GenerationType.UUID)` в твоём `Shop.java` и перейти к `ShopRepository`.
