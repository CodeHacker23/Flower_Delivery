## 🧨 ENUM В JAVA И РОЛИ ПОЛЬЗОВАТЕЛЕЙ (ROLE) — ГАЙД ДЛЯ ТЕХ, КТО НЕ ХОЧЕТ БЫТЬ РАБОМ СТРОК

Это не «гайд как на хабре», это методичка в стиле «чтоб даже после трёх дней запоя и двух проституток ты всё равно помнил, что такое enum и нахера он нужен».

---

## 1. БОЛЬ БЕЗ ENUM: КОГДА ВСЁ НА СТРОКАХ

Представь, что ты делаешь систему с ролями пользователей: курьер, магазин, админ.

### Вариант тупого мира: всё через `String`

```java
String role = "COURIER";
```

Что может пойти не так?

- Написал `"COURIR"` вместо `"COURIER"` — **компилятор молчит**, потому что это просто строка.
- Где-то в коде один умник решил, что `"DELIVERY"` звучит красивее, и поменял только в одном месте.
- В БД лежит `"COURIER"`, в коде ты сравниваешь с `"KURYER"`, и сидишь потом, как сутенёр без девочек, и не понимаешь, почему всё упало.

Ты начинаешь плодить по коду вот это:

```java
if ("COURIER".equals(user.getRole())) { ... }
if ("SHOP".equals(user.getRole())) { ... }
if ("ADMIN".equals(user.getRole())) { ... }
```

А потом кто-то в одном месте поменял `ADMIN` на `SUPER_ADMIN`, в другом забыл, в третьем опечатался — и начинается классическое айтишное блядство:

- «Почему админ не может зайти в админку?»  
- «Почему курьер внезапно может всё удалять?»  
- «Почему роль в БД `ADMNI`?»

Строки в таких вещах — это как давать доступ к кассе человеку без мозгов: **всё вроде работает, пока не ебанет**.

---

## 2. ЧТО ТАКОЕ `enum` НА САМОМ ДЕЛЕ

### 2.1. Человеческими словами

**`enum` = «перечисление фиксированных состояний»**.

Ты говоришь:\
«Вот есть сущность Роль, и она может быть только одной из заранее определённых: `COURIER`, `SHOP`, `ADMIN`. Никаких `BOSS`, `PIMP`, `GOD_OF_SYSTEM` без моего ведома».

То есть вместо:

```java
String role = "COURIER";
```

ты делаешь:

```java
Role role = Role.COURIER;
```

И тут начинается магия:

- Нельзя написать `Role.KURER` — такого просто **нет**.
- Нельзя случайно присвоить `"COURIER"` в `Role` — типы разные, компилятор тебя пошлёт.
- IDE подсказывает тебе все возможные значения: `Role.` → показывает `COURIER`, `SHOP`, `ADMIN`.

### 2.2. Формально (но по-человечески)

```java
public enum Role {
    COURIER,
    SHOP,
    ADMIN
}
```

- `public` — доступен отовсюду (из любого пакета).
- `enum` — говорим Java: «это перечисление, а не обычный класс».
- `Role` — имя типа.\
  Теперь `Role` — такой же тип, как `String`, `int`, `User`.
- Внутри: `COURIER`, `SHOP`, `ADMIN` — это **константы** (объекты типа `Role`).

Под капотом Java делает примерно вот что (упрощённо):

```java
public final class Role extends Enum<Role> {
    public static final Role COURIER = new Role("COURIER", 0);
    public static final Role SHOP    = new Role("SHOP", 1);
    public static final Role ADMIN   = new Role("ADMIN", 2);

    // приватный конструктор
    private Role(String name, int ordinal) {
        super(name, ordinal);
    }
}
```

То есть `enum` — это не «список словечек», а **класс с фиксированным набором объектов**.

---

## 3. НАШИ РОЛИ В ПРОЕКТЕ: ЗАЧЕМ НАМ `Role`

В проекте есть три типа пользователей:

- **Курьер** — бегает, потеет, матерится, таскает коробки.
- **Магазин** — принимает заказы, создаёт новые, смотрит статусы.
- **Админ** — сидит как сутенёр над всей этой тусовкой: банит, даёт доступы, крутит комиссии, штрафы.

Мы уже заложили это в БД:

```sql
role VARCHAR(20) CHECK (role IN ('COURIER', 'SHOP', 'ADMIN'))
```

То есть в таблице `users.role` могут быть только эти три значения.

Теперь нам нужно **в Java** отразить то же самое:\
чтобы в коде была не разъезжающаяся срачка со строками, а **один источник правды — enum `Role`**.

---

## 4. КАК `Role` ЖИВЁТ ВМЕСТЕ С БД

### 4.1. В БД — строка, в Java — enum

В PostgreSQL в колонке `role` лежит строка: `'COURIER'`, `'SHOP'`, `'ADMIN'`.

В Java мы хотим это видеть так:

```java
private Role role;
``>

И работать вот так:

```java
if (user.getRole() == Role.COURIER) {
    // логика для курьера
}
```

За конвертацию туда‑сюда отвечает JPA/Hibernate.

### 4.2. Аннотация `@Enumerated(EnumType.STRING)`

В модели `User` (которую мы будем писать далее) будет примерно так:

```java
@Enumerated(EnumType.STRING)
@Column(name = "role")
private Role role;
```

Разбираем каждое мать его слово:

- `@Enumerated` — аннотация JPA, говорит Hibernate: «это enum, не трогай, работай аккуратно».
- `EnumType.STRING` — **важно**:
  - Храним в БД **строку** (`'COURIER'`), а не номер (`0`, `1`, `2`).
  - Если бы был `ORDINAL`, то:
    - `COURIER = 0`, `SHOP = 1`, `ADMIN = 2`.
    - Ты вставляешь посередине `MANAGER`, и всё, половина БД превращается в тыкву.
- `@Column(name = "role")` — указывает, какая колонка в БД будет соответствовать этому полю.

Итого:\
БД видит `"COURIER"`, Java видит `Role.COURIER`, а Hibernate крутится посредине как переводчик между двумя бухими клиентами.

---

## 5. ПОЛНЫЙ ПРИМЕР `Role` С КОММЕНТАМИ

Дальше — **эталонный** пример того, как мы будем писать `Role.java` в проекте.  
Не копипасть слепо — а **понимай каждое слово**.

```java
package org.example.flower_delivery.model;
```

- `package` — объявление пакета.
- `org.example.flower_delivery.model` — «папка» логическая, где лежат модели (User, Role и прочие).
- Пакеты нужны, чтобы:
  - не было конфликтов имён (может быть несколько `User` в разных пакетах);
  - наводить порядок (model, service, repository и т.д.).

```java
/**
 * Enum (перечисление) ролей пользователей.
 *
 * Роль определяет, что пользователь может делать в системе:
 * - COURIER: курьер, который бегает и доставляет заказы
 * - SHOP: магазин, который создает заказы и следит за доставкой
 * - ADMIN: админ, который рулит всем этим бардаком
 *
 * Enum используем вместо строк, чтобы:
 * - нельзя было опечататься ("COURIER" vs "COURIR")
 * - IDE подсказвала доступные значения
 * - код был типобезопасным (Role, а не просто String)
 */
public enum Role {
```

- `/** ... */` — Javadoc‑комментарий. IDE может подсасывать это как документацию.
- `public` — enum доступен из других пакетов (нам это нужно, мы будем юзать его в сервисах, репах и т.д.).
- `enum` — ключевое слово: объявляем перечисление.
- `Role` — имя типа. Теперь `Role` — это полноценный тип, как `String` или `User`.

Дальше — перечисление констант:

```java
    /**
     * Роль: Курьер.
     * Курьер — исполнитель доставки:
     * - видит доступные заказы
     * - может брать заказы
     * - меняет статусы (В магазине, Забрал, В пути, Вручил)
     */
    COURIER,
```

- `COURIER` — константа enum.
- Запятая `,` — потому что дальше будут другие значения.
- Комментарий сверху — чтобы даже через год, когда забудешь нахер всё, быстро вспомнить, что это за роль.

```java
    /**
     * Роль: Магазин.
     * Магазин:
     * - создает заказы
     * - видит статусы заказов
     * - может видеть, где курьер
     */
    SHOP,
```

- То же самое, только для магазина.

```java
    /**
     * Роль: Администратор.
     * Админ:
     * - активирует/блокирует курьеров и магазины
     * - управляет заказами
     * - настраивает комиссии, штрафы, депозиты
     */
    ADMIN
}
```

- `ADMIN` — последняя константа, **без запятой**, потому что список заканчивается.
- `}` — конец объявления enum.

---

## 6. КАК ЭТО БУДЕТ ИСПОЛЬЗОВАТЬСЯ В КОДЕ

### 6.1. В модели `User`

```java
@Enumerated(EnumType.STRING)
@Column(name = "role")
private Role role;
```

- В БД: `'COURIER'`.
- В Java: `Role.COURIER`.

### 6.2. В репозитории

```java
List<User> findByRole(Role role);
List<User> findByRoleAndIsActiveTrue(Role role);
```

Spring Data сам переведет это в SQL:

```sql
SELECT * FROM users WHERE role = 'COURIER' AND is_active = true;
```

### 6.3. В сервисе проверка прав

```java
public boolean isAdmin(Long telegramId) {
    Optional<Role> roleOpt = getRoleByTelegramId(telegramId);
    return roleOpt.isPresent() && roleOpt.get() == Role.ADMIN;
}
```

- `Role.ADMIN` — читается как нормальное человеческое значение, а не `"ADMIN"`.
- Если завтра появится `SUPER_ADMIN` — ты добавишь его в enum и IDE тут же покажет, где ещё нужно обновиться.

---

## 7. В ЧЕМ ГЛАВНАЯ МЫСЛЬ ENUM (ЗАПИШИ СЕБЕ В МОЗГ)

1. **Enum = фиксированный список допустимых значений.**
2. Enum защищает от:
   - опечаток в строках,
   - рандомных значений,
   - несогласованности по всему проекту.
3. Enum делает код:
   - типобезопасным,
   - более читаемым,
   - проще для рефакторинга.
4. В связке с JPA/Hibernate:
   - в БД храним строку,
   - в коде работаем с enum,
   - всё туда‑сюда конвертится автоматически.

---

## 8. ЧТО ТЕБЕ СЕЙЧАС НУЖНО СДЕЛАТЬ

1. Создать файл `Role.java` в пакете `org.example.flower_delivery.model`.
2. Написать в нём:
   - `package ...`
   - Javadoc к enum (что это, какие роли, зачем).
   - Сам `enum Role { COURIER, SHOP, ADMIN }` с комментариями к каждой роли.
3. Прислать мне сюда содержимое `Role.java`.
4. Я пройдусь по **каждой строке**, задам тебе вопросы и проверю, что ты не просто «набил код», а **понимаешь**:
   - зачем enum,
   - как он живёт с БД,
   - как его юзать в сервисах и репах.

Не стесняйся материться в комментариях — главное, чтобы **мысли были чёткие**.  
Пиши `Role.java`, потом будем ебашить `User` и дальше собирать нормальную архитектуру.

