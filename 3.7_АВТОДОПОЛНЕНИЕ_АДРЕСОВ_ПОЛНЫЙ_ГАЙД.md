# 🗺️ АВТОДОПОЛНЕНИЕ АДРЕСОВ - ПОЛНЫЙ ГАЙД

**Дата:** 2026-01-26  
**Задача:** Реализовать автодополнение адресов (как в такси) при регистрации магазина

---

## 📋 СОДЕРЖАНИЕ

1. [Что мы хотим сделать](#что-мы-хотим)
2. [Как это работает в Telegram ботах](#как-это-работает)
3. [Варианты реализации](#варианты-реализации)
4. [Интеграция с API геокодинга](#интеграция-с-api)
5. [Реализация для нашего проекта](#реализация)
6. [Пошаговый план](#план)

---

# ЧТО МЫ ХОТИМ

## 🎯 Цель:

**Когда магазин вводит адрес забора товаров:**
1. Пользователь начинает вводить: "ул. Ленина..."
2. Бот предлагает варианты:
   - "ул. Ленина, д. 10" ✅
   - "ул. Ленина, д. 25" ✅
   - "ул. Ленина, д. 50" ✅
3. Пользователь нажимает на вариант → адрес заполняется автоматически
4. Бот получает координаты (lat/long) через API геокодинга

## 🎯 Особенности нашего проекта:

- **Один бот = один город/регион** (не нужно указывать город)
- **Пользователи вводят адрес без города** (только улица, дом)
- **Автодополнение работает локально** (для конкретного города)

---

# КАК ЭТО РАБОТАЕТ В TELEGRAM БОТАХ

## 🔍 Варианты реализации:

### 1. **InlineQuery** (сложнее, но мощнее)

**Как работает:**
- Пользователь пишет `@botname` в любом чате
- Начинает вводить адрес
- Бот предлагает варианты в выпадающем списке

**Пример:**
```
Пользователь: @FlowerDeliveryBot ул. Ленина...
Бот показывает:
  📍 ул. Ленина, д. 10
  📍 ул. Ленина, д. 25
  📍 ул. Ленина, д. 50
```

**Плюсы:**
- Работает в любом чате (не только в диалоге с ботом)
- Удобно для пользователей

**Минусы:**
- Сложнее реализовать
- Нужно обрабатывать `InlineQuery` в `Bot.java`
- Требует настройки через @BotFather

---

### 2. **Кнопки с вариантами** (проще, для нашего случая идеально!)

**Как работает:**
- Пользователь вводит часть адреса в диалоге с ботом
- Бот отправляет сообщение с кнопками (варианты адресов)
- Пользователь нажимает кнопку → адрес заполняется

**Пример:**
```
Бот: Введите адрес забора товаров:
Пользователь: ул. Ленина

Бот: Выберите адрес:
  [ул. Ленина, д. 10]
  [ул. Ленина, д. 25]
  [ул. Ленина, д. 50]
  [Ввести другой адрес]

Пользователь нажимает "ул. Ленина, д. 10"
Бот: ✅ Адрес выбран: ул. Ленина, д. 10
     📍 Координаты: 55.75581400, 37.61730000
```

**Плюсы:**
- ✅ Проще реализовать (используем `InlineKeyboardMarkup`)
- ✅ Работает в диалоге с ботом (идеально для регистрации)
- ✅ Не требует настройки через @BotFather
- ✅ Можно показывать до 100 кнопок (достаточно для вариантов)

**Минусы:**
- Работает только в диалоге с ботом (но нам этого достаточно!)

---

### 3. **ForceReply** (альтернатива)

**Как работает:**
- Бот отправляет сообщение с `ForceReply`
- Пользователь вводит адрес
- Бот обрабатывает и предлагает варианты

**Плюсы:**
- Просто реализовать

**Минусы:**
- Менее удобно для пользователя
- Нет автодополнения "на лету"

---

## 🎯 НАШ ВЫБОР: **Кнопки с вариантами** (вариант 2)

**Почему:**
- Проще реализовать
- Идеально для пошаговой регистрации магазина
- Не требует настройки через @BotFather
- Можно интегрировать с API геокодинга

---

# ВАРИАНТЫ РЕАЛИЗАЦИИ

## Вариант А: База данных популярных адресов (для начала)

**Как работает:**
1. Создаем таблицу `popular_addresses` с популярными адресами города
2. Пользователь вводит "ул. Ленина"
3. Бот ищет в БД: `WHERE address LIKE 'ул. Ленина%'`
4. Показывает первые 10 вариантов кнопками

**Плюсы:**
- ✅ Не требует API (бесплатно)
- ✅ Быстро работает (поиск в БД)
- ✅ Можно добавить адреса вручную

**Минусы:**
- Нужно поддерживать базу адресов
- Не все адресы будут в базе

**Когда использовать:**
- Для MVP (начальная версия)
- Если адресов немного (один город)

---

## Вариант Б: API геокодинга (2GIS / Яндекс)

**Как работает:**
1. Пользователь вводит "ул. Ленина"
2. Бот отправляет запрос к API: `https://catalog.api.2gis.com/3.0/items/geocode?q=ул. Ленина&city=Москва`
3. API возвращает варианты адресов
4. Бот показывает кнопки с вариантами

**Плюсы:**
- ✅ Все адреса города (не нужно поддерживать базу)
- ✅ Точные координаты (lat/long) сразу
- ✅ Актуальные данные (API обновляется автоматически)

**Минусы:**
- Требует API ключ (но есть бесплатные лимиты)
- Зависимость от внешнего сервиса

**Когда использовать:**
- Для продакшена (финальная версия)
- Когда нужны все адреса города

---

## Вариант В: Гибридный (БД + API)

**Как работает:**
1. Сначала ищем в БД популярных адресов
2. Если найдено < 5 вариантов → запрашиваем API
3. Показываем все варианты (из БД + из API)

**Плюсы:**
- ✅ Быстро для популярных адресов (из БД)
- ✅ Полнота для редких адресов (из API)
- ✅ Экономия запросов к API (популярные адреса из БД)

**Минусы:**
- Сложнее реализовать (два источника данных)

**Когда использовать:**
- Для оптимизации (меньше запросов к API)
- Когда есть популярные адреса (часто используемые)

---

## 🎯 НАШ ВЫБОР: **Вариант Б (API геокодинга)**

**Почему:**
- Проще реализовать (один источник данных)
- Все адреса города (не нужно поддерживать базу)
- Точные координаты сразу (не нужно геокодировать потом)

**Для MVP можно начать с Варианта А (БД), потом перейти на API**

---

# ИНТЕГРАЦИЯ С API ГЕОКОДИНГА

## 🔍 Доступные API:

### 1. **2GIS API** (рекомендуем для России!)

**Бесплатный лимит:**
- 1000 запросов/день (достаточно для начала)
- Платно: от 0.5₽ за запрос

**Документация:**
- https://docs.2gis.com/ru/api/search/overview

**Пример запроса:**
```http
GET https://catalog.api.2gis.com/3.0/items/geocode
?q=ул. Ленина
&city=Москва
&key=YOUR_API_KEY
```

**Ответ:**
```json
{
  "result": {
    "items": [
      {
        "name": "ул. Ленина, д. 10",
        "point": {
          "lat": 55.75581400,
          "lon": 37.61730000
        }
      },
      {
        "name": "ул. Ленина, д. 25",
        "point": {
          "lat": 55.75600000,
          "lon": 37.61800000
        }
      }
    ]
  }
}
```

**Плюсы:**
- ✅ Хорошо работает для России
- ✅ Бесплатный лимит (1000/день)
- ✅ Точные координаты

---

### 2. **Яндекс.Геокодер API**

**Бесплатный лимит:**
- 10000 запросов/день (больше чем у 2GIS!)

**Документация:**
- https://yandex.ru/dev/geocoder/doc/ru/

**Пример запроса:**
```http
GET https://geocode-maps.yandex.ru/1.x/
?apikey=YOUR_API_KEY
&geocode=Москва, ул. Ленина
&format=json
```

**Плюсы:**
- ✅ Большой бесплатный лимит (10000/день)
- ✅ Хорошо работает для России

**Минусы:**
- Сложнее парсить ответ (больше данных)

---

### 3. **Google Places API**

**Бесплатный лимит:**
- $200 кредитов/месяц (примерно 40000 запросов)

**Плюсы:**
- ✅ Работает везде (не только Россия)
- ✅ Очень точные данные

**Минусы:**
- Платно (после лимита)
- Требует карту (Google Maps)

---

## 🎯 НАШ ВЫБОР: **2GIS API** (для России)

**Почему:**
- Бесплатный лимит (1000/день) - достаточно для начала
- Хорошо работает для России
- Простой API (легко интегрировать)
- Точные координаты

**Альтернатива:** Яндекс.Геокодер (если нужен больший лимит)

---

# РЕАЛИЗАЦИЯ

## 📁 Структура кода:

```
src/main/java/org/example/flower_delivery/
├── service/
│   ├── GeocodingService.java          # Сервис для работы с API геокодинга
│   └── AddressSuggestionService.java  # Сервис для автодополнения адресов
├── handler/
│   └── ShopRegistrationHandler.java    # Обработчик регистрации магазина
└── dto/
    └── AddressSuggestion.java          # DTO для варианта адреса
```

---

## 🔧 Что создаем:

### 1. **GeocodingService.java**

**Задача:** Превращать адрес в координаты (lat/long)

**Методы:**
- `geocodeAddress(String address, String city)` → `Coordinates`
- `searchAddresses(String query, String city)` → `List<AddressSuggestion>`

**Пример:**
```java
@Service
public class GeocodingService {
    
    @Value("${geocoding.2gis.api.key}")
    private String apiKey;
    
    public List<AddressSuggestion> searchAddresses(String query, String city) {
        // 1. Отправляем запрос к 2GIS API
        // 2. Парсим ответ (JSON)
        // 3. Возвращаем список вариантов адресов
    }
    
    public Coordinates geocodeAddress(String address, String city) {
        // 1. Отправляем запрос к 2GIS API
        // 2. Получаем координаты (lat/long)
        // 3. Возвращаем Coordinates
    }
}
```

---

### 2. **AddressSuggestionService.java**

**Задача:** Управлять автодополнением адресов

**Методы:**
- `getSuggestions(String query, String city)` → `List<AddressSuggestion>`
- `createSuggestionButtons(List<AddressSuggestion>)` → `InlineKeyboardMarkup`

**Пример:**
```java
@Service
public class AddressSuggestionService {
    
    private final GeocodingService geocodingService;
    
    public InlineKeyboardMarkup createSuggestionButtons(String query, String city) {
        // 1. Получаем варианты адресов из GeocodingService
        // 2. Создаем кнопки (InlineKeyboardMarkup)
        // 3. Возвращаем клавиатуру
    }
}
```

---

### 3. **ShopRegistrationHandler.java**

**Задача:** Обрабатывать регистрацию магазина (пошаговый диалог)

**Шаги:**
1. Пользователь пишет `/register_shop`
2. Бот: "Введите название магазина:"
3. Пользователь: "Цветы на Ленина"
4. Бот: "Введите адрес забора товаров:"
5. **Пользователь: "ул. Ленина"** ← здесь автодополнение!
6. **Бот показывает кнопки с вариантами адресов**
7. Пользователь нажимает кнопку → адрес заполняется
8. Бот сохраняет магазин в БД

---

## 📝 DTO (Data Transfer Object):

### AddressSuggestion.java

```java
@Getter
@Setter
@Builder
public class AddressSuggestion {
    private String address;        // "ул. Ленина, д. 10"
    private Double latitude;       // 55.75581400
    private Double longitude;       // 37.61730000
    private String fullAddress;    // "Москва, ул. Ленина, д. 10" (полный адрес)
}
```

---

## 🔧 Интеграция в Bot.java:

```java
@Override
public void onUpdateReceived(Update update) {
    // Обработка callback query (нажатия на кнопки)
    if (update.hasCallbackQuery()) {
        CallbackQuery callbackQuery = update.getCallbackQuery();
        String callbackData = callbackQuery.getData();
        
        // Если это выбор адреса: "address_55.75581400_37.61730000"
        if (callbackData.startsWith("address_")) {
            shopRegistrationHandler.handleAddressSelection(update);
            return;
        }
        
        callbackQueryHandler.handle(update);
        return;
    }
    
    // Обработка текстовых сообщений
    if (update.hasMessage() && update.getMessage().hasText()) {
        String text = update.getMessage().getText();
        
        if (text.equals("/start")) {
            startCommandHandler.handle(update);
        } else if (text.equals("/register_shop")) {
            shopRegistrationHandler.handle(update);
        } else {
            // Если пользователь в процессе регистрации магазина
            // и вводит адрес → показываем автодополнение
            shopRegistrationHandler.handleAddressInput(update);
        }
    }
}
```

---

# ПЛАН РЕАЛИЗАЦИИ

## Этап 1: Базовая структура (без API)

1. ✅ Создать `AddressSuggestion.java` (DTO)
2. ✅ Создать `ShopRegistrationHandler.java` (обработчик)
3. ✅ Реализовать пошаговый диалог (без автодополнения)
4. ✅ Сохранение магазина в БД

**Результат:** Регистрация магазина работает, но адрес вводится вручную

---

## Этап 2: Автодополнение (с API)

1. ✅ Создать `GeocodingService.java`
2. ✅ Интегрировать 2GIS API
3. ✅ Создать `AddressSuggestionService.java`
4. ✅ Добавить автодополнение в `ShopRegistrationHandler`
5. ✅ Показывать кнопки с вариантами адресов

**Результат:** Автодополнение адресов работает!

---

## Этап 3: Оптимизация

1. ✅ Кэширование популярных адресов (Redis или БД)
2. ✅ Обработка ошибок API (fallback на ручной ввод)
3. ✅ Валидация адресов (проверка, что адрес существует)

**Результат:** Быстрое и надежное автодополнение

---

# ПРИМЕР РАБОТЫ

## Сценарий 1: Успешное автодополнение

```
Пользователь: /register_shop
Бот: Введите название магазина:
Пользователь: Цветы на Ленина
Бот: Введите адрес забора товаров:
     (начните вводить адрес, и я предложу варианты)

Пользователь: ул. Ленина

Бот: Выберите адрес:
     [📍 ул. Ленина, д. 10]
     [📍 ул. Ленина, д. 25]
     [📍 ул. Ленина, д. 50]
     [✏️ Ввести адрес вручную]

Пользователь нажимает: "ул. Ленина, д. 10"

Бот: ✅ Адрес выбран: ул. Ленина, д. 10
     📍 Координаты получены
     Введите телефон магазина (необязательно):
```

---

## Сценарий 2: Адрес не найден

```
Пользователь: ул. НесуществующаяУлица

Бот: ❌ Адреса не найдены
     [✏️ Ввести адрес вручную]

Пользователь нажимает: "Ввести адрес вручную"

Бот: Введите полный адрес:
Пользователь: ул. НесуществующаяУлица, д. 999
Бот: ✅ Адрес сохранен (координаты будут получены позже)
```

---

## Сценарий 3: Много вариантов (>10)

```
Пользователь: ул. Ленина

Бот: Найдено 25 вариантов. Показываю первые 10:
     [📍 ул. Ленина, д. 10]
     [📍 ул. Ленина, д. 25]
     ...
     [📍 ул. Ленина, д. 100]
     [➡️ Показать еще (11-20)]
     [✏️ Ввести адрес вручную]

Пользователь нажимает: "Показать еще"

Бот: Варианты 11-20:
     [📍 ул. Ленина, д. 101]
     ...
```

---

# КОНФИГУРАЦИЯ

## application.properties:

```properties
# ============================================
# ГЕОКОДИНГ (2GIS API)
# ============================================
# API ключ для 2GIS (получить на https://dev.2gis.com/)
geocoding.2gis.api.key=${TWOGIS_API_KEY}
geocoding.2gis.api.url=https://catalog.api.2gis.com/3.0/items/geocode

# Город по умолчанию (для автодополнения)
geocoding.default.city=Москва

# Лимит вариантов адресов (сколько показывать)
geocoding.suggestions.limit=10
```

---

# БЕЗОПАСНОСТЬ

## 🔒 Важные моменты:

1. **API ключ в переменных окружения:**
   - Не хранить в `application.properties` (только `${TWOGIS_API_KEY}`)
   - Добавить в `.gitignore`

2. **Валидация входных данных:**
   - Проверять, что `query` не пустой
   - Ограничить длину `query` (максимум 100 символов)
   - Санитизировать `query` (убрать спецсимволы)

3. **Обработка ошибок API:**
   - Если API недоступен → fallback на ручной ввод
   - Логировать ошибки API
   - Не показывать пользователю технические детали ошибок

---

# ИТОГО

## ✅ Что мы реализуем:

1. **Автодополнение адресов** через кнопки (InlineKeyboardMarkup)
2. **Интеграция с 2GIS API** для поиска адресов
3. **Автоматическое получение координат** (lat/long)
4. **Fallback на ручной ввод** если адрес не найден

## 🎯 Преимущества:

- ✅ Удобно для пользователей (как в такси)
- ✅ Точные координаты сразу (не нужно геокодировать потом)
- ✅ Меньше ошибок (пользователь выбирает из вариантов)

---

**Следующий шаг:** Создать `GeocodingService.java` и интегрировать 2GIS API 🚀
