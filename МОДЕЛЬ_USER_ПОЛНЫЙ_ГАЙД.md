# 🧑‍💻 МОДЕЛЬ USER — ПОЛНЫЙ ГАЙД (КАК СВЯЗАТЬ БД С JAVA КОДОМ)

**Версия:** 1.0  
**Для кого:** Для тех, кто хочет понять JPA, Hibernate и маппинг БД ↔ Java  
**Стиль:** Максимально подробно, с матом, юмором и объяснением каждой строчки

---

## 🎯 ЧТО МЫ ДЕЛАЕМ

Создаём Java класс `User`, который:
- **Маппится** на таблицу `users` в PostgreSQL
- **Автоматически** превращается в SQL запросы через Hibernate
- **Позволяет** работать с БД как с обычными Java объектами

**Простыми словами:**  
Вместо того чтобы писать SQL вручную (`SELECT * FROM users WHERE telegram_id = 123`), ты пишешь Java код (`userRepository.findByTelegramId(123L)`), а Hibernate сам переводит это в SQL.

---

## 📊 СВЯЗЬ МЕЖДУ БД И JAVA

### В БД (PostgreSQL):

```sql
CREATE TABLE users (
    id UUID PRIMARY KEY,
    telegram_id BIGINT UNIQUE NOT NULL,
    full_name VARCHAR(255) NOT NULL,
    phone VARCHAR(50),
    role VARCHAR(20),
    is_active BOOLEAN DEFAULT FALSE NOT NULL,
    created_at TIMESTAMP NOT NULL,
    updated_at TIMESTAMP NOT NULL
);
```

### В Java (модель User):

```java
@Entity
@Table(name = "users")
public class User {
    @Id
    private UUID id;
    
    @Column(name = "telegram_id", unique = true, nullable = false)
    private Long telegramId;
    
    @Column(name = "full_name", nullable = false)
    private String fullName;
    
    // ... и так далее
}
```

**Hibernate автоматически связывает:**
- `id` (Java) ↔ `id` (БД)
- `telegramId` (Java) ↔ `telegram_id` (БД)
- `fullName` (Java) ↔ `full_name` (БД)

---

## 🔧 ЧТО ТАКОЕ JPA И HIBERNATE (ПРОСТЫМИ СЛОВАМИ)

### JPA (Java Persistence API)

**Что это:**  
Стандарт Java для работы с БД. Это **интерфейс** (контракт), который говорит: "Вот как нужно работать с БД в Java".

**Аналогия:**  
JPA — это как правила дорожного движения. Все водители (разработчики) должны их соблюдать, но каждый может ехать на своей машине (Hibernate, EclipseLink, OpenJPA).

---

### Hibernate

**Что это:**  
**Реализация** JPA. Это конкретная библиотека, которая делает всю магию:
- Переводит Java код в SQL
- Маппит Java объекты на таблицы БД
- Управляет транзакциями
- Кэширует данные

**Аналогия:**  
Hibernate — это как конкретная марка машины (например, BMW). Она следует правилам дорожного движения (JPA), но имеет свои фишки и особенности.

---

### Как это работает вместе:

1. **Ты пишешь Java код:**
   ```java
   User user = new User();
   user.setTelegramId(123L);
   user.setFullName("Иван Иванов");
   userRepository.save(user);
   ```

2. **Hibernate переводит это в SQL:**
   ```sql
   INSERT INTO users (telegram_id, full_name, ...) 
   VALUES (123, 'Иван Иванов', ...);
   ```

3. **PostgreSQL выполняет SQL и возвращает результат**

4. **Hibernate превращает результат обратно в Java объект**

**Всё автоматически!** Ты не пишешь SQL, Hibernate делает это за тебя.

---

## 📝 АННОТАЦИИ JPA (РАЗБОР КАЖДОЙ)

### `@Entity` — "Это таблица в БД!"

**Что делает:**  
Говорит Hibernate: "Этот класс — это таблица в БД. Маппь его на таблицу!"

**Пример:**
```java
@Entity
public class User {
    // ...
}
```

**Без `@Entity`:**  
Hibernate проигнорирует этот класс и не будет создавать SQL запросы.

**С `@Entity`:**  
Hibernate понимает: "Ок, это таблица. Найду её в БД и буду работать с ней."

---

### `@Table(name = "users")` — "Название таблицы"

**Что делает:**  
Указывает Hibernate, какая именно таблица в БД соответствует этому классу.

**Пример:**
```java
@Entity
@Table(name = "users")
public class User {
    // ...
}
```

**Без `@Table`:**  
Hibernate будет искать таблицу с именем класса (`User` → таблица `user`).

**С `@Table(name = "users")`:**  
Hibernate точно знает: "Ищу таблицу `users`, а не `user` или `User`."

**Почему важно:**  
В Java классы пишутся `PascalCase` (`User`), а в БД таблицы часто `snake_case` (`users`). `@Table` связывает их.

---

### `@Id` — "Это первичный ключ!"

**Что делает:**  
Говорит Hibernate: "Это поле — первичный ключ таблицы (PRIMARY KEY)."

**Пример:**
```java
@Id
@GeneratedValue(strategy = GenerationType.UUID)
private UUID id;
```

**Без `@Id`:**  
Hibernate не поймёт, какое поле уникально идентифицирует запись. Не сможет делать `findById()`, `update()`, `delete()`.

**С `@Id`:**  
Hibernate понимает: "Это поле — уникальный идентификатор. По нему буду искать, обновлять, удалять записи."

**Аналогия:**  
Как номер паспорта — по нему можно однозначно найти человека.

---

### `@GeneratedValue(strategy = GenerationType.UUID)` — "Автогенерация UUID"

**Что делает:**  
Говорит Hibernate: "Не жди, пока я укажу ID. PostgreSQL сам сгенерирует UUID при создании записи."

**Пример:**
```java
@Id
@GeneratedValue(strategy = GenerationType.UUID)
private UUID id;
```

**Стратегии генерации:**
- `GenerationType.UUID` — PostgreSQL сам генерирует UUID (через `gen_random_uuid()`)
- `GenerationType.IDENTITY` — БД сама генерирует (AUTO_INCREMENT в MySQL, SERIAL в PostgreSQL)
- `GenerationType.SEQUENCE` — использует sequence в БД
- `GenerationType.TABLE` — использует отдельную таблицу для генерации ID

**Почему UUID:**
- Уникален глобально (не может быть двух одинаковых)
- Безопаснее (нельзя угадать следующий ID)
- Подходит для распределенных систем

**Без `@GeneratedValue`:**  
Придётся вручную генерировать UUID перед сохранением:
```java
user.setId(UUID.randomUUID()); // Хуйня, не делай так!
```

**С `@GeneratedValue`:**  
Hibernate сам попросит PostgreSQL сгенерировать UUID:
```java
userRepository.save(user); // Hibernate сам сгенерирует ID!
```

---

### `@Column(name = "telegram_id", unique = true, nullable = false)` — "Маппинг на колонку БД"

**Что делает:**  
Связывает Java поле с колонкой в БД и указывает её свойства.

**Пример:**
```java
@Column(name = "telegram_id", unique = true, nullable = false)
private Long telegramId;
```

**Параметры:**
- `name = "telegram_id"` — название колонки в БД (snake_case)
- `unique = true` — колонка уникальна (UNIQUE в БД)
- `nullable = false` — колонка обязательна (NOT NULL в БД)

**Без `@Column`:**  
Hibernate будет искать колонку с именем поля (`telegramId` → `telegramId` или `telegram_id`). Может не найти, если названия не совпадают.

**С `@Column(name = "telegram_id")`:**  
Hibernate точно знает: "Java поле `telegramId` → БД колонка `telegram_id`."

**Почему важно:**  
В Java поля пишутся `camelCase` (`telegramId`), а в БД колонки часто `snake_case` (`telegram_id`). `@Column` связывает их.

---

### `@Enumerated(EnumType.STRING)` — "Enum как строка в БД"

**Что делает:**  
Говорит Hibernate: "Сохраняй enum как строку в БД (`'COURIER'`), а не как число (`0`)."

**Пример:**
```java
@Enumerated(EnumType.STRING)
@Column(name = "role")
private Role role;
```

**Варианты:**
- `EnumType.STRING` — сохраняет как строку (`'COURIER'`, `'SHOP'`, `'ADMIN'`)
- `EnumType.ORDINAL` — сохраняет как число (`0`, `1`, `2`) — **НЕ ИСПОЛЬЗУЙ!**

**Почему `EnumType.STRING`:**
- Читаемо в БД (видишь `'COURIER'`, а не `0`)
- Устойчиво к изменениям (если добавишь новую роль в середину, старые данные не сломаются)
- Легче дебажить (смотришь в БД и сразу видишь роль)

**Почему НЕ `EnumType.ORDINAL`:**
- Нечитаемо в БД (видишь `0`, `1`, `2` — не понятно что это)
- Хрупко (если добавишь `MANAGER` между `COURIER` и `SHOP`, все старые данные поедут к чертям)

**Без `@Enumerated`:**  
Hibernate будет использовать `EnumType.ORDINAL` по умолчанию (плохо!).

**С `@Enumerated(EnumType.STRING)`:**  
Hibernate сохраняет enum как строку, как мы и хотим.

---

### `@CreationTimestamp` — "Автоматически ставить дату создания"

**Что делает:**  
Hibernate автоматически устанавливает текущую дату и время при создании записи.

**Пример:**
```java
@CreationTimestamp
@Column(name = "created_at", nullable = false, updatable = false)
private LocalDateTime createdAt;
```

**Параметры `@Column`:**
- `updatable = false` — нельзя изменить после создания (защита от случайного изменения)

**Без `@CreationTimestamp`:**  
Придётся вручную ставить дату:
```java
user.setCreatedAt(LocalDateTime.now()); // Хуйня, не делай так!
```

**С `@CreationTimestamp`:**  
Hibernate сам ставит дату при сохранении:
```java
userRepository.save(user); // Hibernate сам поставит createdAt!
```

---

### `@UpdateTimestamp` — "Автоматически обновлять дату изменения"

**Что делает:**  
Hibernate автоматически обновляет дату и время при каждом изменении записи.

**Пример:**
```java
@UpdateTimestamp
@Column(name = "updated_at", nullable = false)
private LocalDateTime updatedAt;
```

**Без `@UpdateTimestamp`:**  
Придётся вручную обновлять дату при каждом изменении:
```java
user.setFullName("Новое имя");
user.setUpdatedAt(LocalDateTime.now()); // Хуйня, не делай так!
userRepository.save(user);
```

**С `@UpdateTimestamp`:**  
Hibernate сам обновляет дату при каждом изменении:
```java
user.setFullName("Новое имя");
userRepository.save(user); // Hibernate сам обновит updatedAt!
```

---

## 🛠️ LOMBOK АННОТАЦИИ (ЧТОБЫ НЕ ПИСАТЬ БОЙЛЕРПЛЕЙТ)

### `@Getter` — "Автоматически создай getter методы"

**Что делает:**  
Lombok автоматически создаёт методы `getId()`, `getTelegramId()`, `getFullName()` и т.д.

**Пример:**
```java
@Getter
public class User {
    private UUID id;
    private Long telegramId;
}
```

**Без `@Getter`:**  
Придётся вручную писать:
```java
public UUID getId() {
    return id;
}

public Long getTelegramId() {
    return telegramId;
}
// ... и так для каждого поля. Хуйня!
```

**С `@Getter`:**  
Lombok сам создаст все getter методы. Ты просто пишешь `user.getId()`, и всё работает.

---

### `@Setter` — "Автоматически создай setter методы"

**Что делает:**  
Lombok автоматически создаёт методы `setId()`, `setTelegramId()`, `setFullName()` и т.д.

**Пример:**
```java
@Setter
public class User {
    private UUID id;
    private Long telegramId;
}
```

**Без `@Setter`:**  
Придётся вручную писать:
```java
public void setId(UUID id) {
    this.id = id;
}

public void setTelegramId(Long telegramId) {
    this.telegramId = telegramId;
}
// ... и так для каждого поля. Хуйня!
```

**С `@Setter`:**  
Lombok сам создаст все setter методы. Ты просто пишешь `user.setTelegramId(123L)`, и всё работает.

---

### `@NoArgsConstructor` — "Конструктор без параметров"

**Что делает:**  
Lombok создаёт конструктор без параметров: `new User()`

**Зачем нужен:**  
Hibernate требует конструктор без параметров для создания объектов из БД.

**Пример:**
```java
@NoArgsConstructor
public class User {
    // ...
}
```

**Без `@NoArgsConstructor`:**  
Если у тебя есть конструктор с параметрами, Hibernate не сможет создать объект из БД (нужен конструктор без параметров).

**С `@NoArgsConstructor`:**  
Lombok создаст конструктор `public User() {}`, и Hibernate сможет работать.

---

### `@AllArgsConstructor` — "Конструктор со всеми параметрами"

**Что делает:**  
Lombok создаёт конструктор со всеми полями: `new User(id, telegramId, fullName, ...)`

**Зачем нужен:**  
Удобно для тестов и создания объектов вручную.

**Пример:**
```java
@AllArgsConstructor
public class User {
    private UUID id;
    private Long telegramId;
    private String fullName;
}
```

**Использование:**
```java
User user = new User(id, telegramId, fullName, phone, role, isActive, createdAt, updatedAt);
```

---

### `@Builder` — "Паттерн Builder для создания объектов"

**Что делает:**  
Lombok создаёт Builder для удобного создания объектов.

**Пример:**
```java
@Builder
public class User {
    private UUID id;
    private Long telegramId;
    private String fullName;
}
```

**Использование:**
```java
User user = User.builder()
    .telegramId(123L)
    .fullName("Иван Иванов")
    .isActive(false)
    .build();
```

**Почему удобно:**
- Не нужно помнить порядок параметров конструктора
- Можно указывать только нужные поля
- Читаемо и понятно

**Без `@Builder`:**  
Придётся использовать конструктор или сеттеры:
```java
User user = new User();
user.setTelegramId(123L);
user.setFullName("Иван Иванов");
user.setIsActive(false);
// ... и так далее. Хуйня!
```

**С `@Builder`:**  
Красиво и удобно:
```java
User user = User.builder()
    .telegramId(123L)
    .fullName("Иван Иванов")
    .isActive(false)
    .build();
```

---

## 📋 ПОЛНЫЙ КОД МОДЕЛИ USER

Вот как будет выглядеть полная модель `User`:

```java
package org.example.flower_delivery.model;

import jakarta.persistence.*;
import lombok.*;
import org.hibernate.annotations.CreationTimestamp;
import org.hibernate.annotations.UpdateTimestamp;

import java.time.LocalDateTime;
import java.util.UUID;

/**
 * Модель пользователя (User) - это Java представление таблицы users в БД
 * 
 * @Entity - говорит Hibernate: "Это таблица в БД!"
 * @Table(name = "users") - название таблицы в PostgreSQL
 */
@Entity
@Table(name = "users")
@Getter  // Lombok: автоматически создаст getter методы
@Setter  // Lombok: автоматически создаст setter методы
@NoArgsConstructor  // Lombok: конструктор без параметров (нужен для Hibernate)
@AllArgsConstructor  // Lombok: конструктор со всеми параметрами
@Builder  // Lombok: паттерн Builder для удобного создания объектов
public class User {
    
    /**
     * ID пользователя (UUID - уникальный идентификатор)
     * 
     * @Id - это поле является первичным ключом (PRIMARY KEY)
     * @GeneratedValue(strategy = GenerationType.UUID) - PostgreSQL сам генерирует UUID
     */
    @Id
    @GeneratedValue(strategy = GenerationType.UUID)
    @Column(name = "id", nullable = false, updatable = false)
    private UUID id;
    
    /**
     * Telegram ID пользователя (уникальный ID в Telegram)
     * 
     * @Column(unique = true, nullable = false) - в БД не может быть двух пользователей с одинаковым telegram_id
     */
    @Column(name = "telegram_id", unique = true, nullable = false)
    private Long telegramId;
    
    /**
     * ФИО пользователя
     */
    @Column(name = "full_name", nullable = false)
    private String fullName;
    
    /**
     * Телефон пользователя (может быть NULL)
     */
    @Column(name = "phone")
    private String phone;
    
    /**
     * Роль пользователя (COURIER, SHOP, ADMIN)
     * 
     * @Enumerated(EnumType.STRING) - сохраняем в БД как строку ("COURIER"), а не как число (0)
     */
    @Enumerated(EnumType.STRING)
    @Column(name = "role")
    private Role role;
    
    /**
     * Флаг активности (по умолчанию false - неактивен, админ активирует)
     * 
     * @Builder.Default - говорит Lombok: используй это значение по умолчанию при создании через Builder
     */
    @Column(name = "is_active", nullable = false)
    @Builder.Default
    private Boolean isActive = false;
    
    /**
     * Дата создания (автоматически заполняется при создании записи)
     * 
     * @CreationTimestamp - Hibernate автоматически установит текущую дату при создании
     * updatable = false - нельзя изменить после создания
     */
    @CreationTimestamp
    @Column(name = "created_at", nullable = false, updatable = false)
    private LocalDateTime createdAt;
    
    /**
     * Дата обновления (автоматически обновляется при изменении записи)
     * 
     * @UpdateTimestamp - Hibernate автоматически обновит при каждом изменении
     */
    @UpdateTimestamp
    @Column(name = "updated_at", nullable = false)
    private LocalDateTime updatedAt;
}
```

---

## 🎯 ЗАДАНИЕ ДЛЯ ТЕБЯ

**Создай файл `src/main/java/org/example/flower_delivery/model/User.java` и напиши туда модель `User`!**

Используй код выше как шаблон, но:
- **Не копируй слепо** — понимай каждую строчку
- **Читай комментарии** — они объясняют зачем каждая аннотация
- **Задавай вопросы** — если что-то непонятно

После того как напишешь — пришли код, и я разберу каждую строчку и проверю твоё понимание!

---

## ❓ ВОПРОСЫ ДЛЯ ПРОВЕРКИ (БУДЬ ГОТОВ!)

1. **Зачем нужна аннотация `@Entity`?** (чтобы Hibernate знал, что это таблица в БД)

2. **Зачем нужна аннотация `@Table(name = "users")`?** (чтобы указать точное название таблицы в БД)

3. **Зачем нужна аннотация `@Id`?** (чтобы Hibernate знал, какое поле — первичный ключ)

4. **Зачем нужна аннотация `@Column(name = "telegram_id")`?** (чтобы связать Java поле с колонкой в БД)

5. **Зачем нужна аннотация `@Enumerated(EnumType.STRING)`?** (чтобы сохранять enum как строку, а не как число)

6. **Зачем нужны Lombok аннотации (`@Getter`, `@Setter`, `@Builder`)?** (чтобы не писать бойлерплейт код вручную)

---

**Пиши код, блять! Не копируй — понимай!** 🚀
